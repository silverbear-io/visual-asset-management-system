name: AWS Code Build
on: [push]
jobs:
  BuildWebJob:
    runs-on: codebuild-VAMS-BuildAndDeploy-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - run: cd ./web && nvm use
      - run: yarn install
      - run: npm run build